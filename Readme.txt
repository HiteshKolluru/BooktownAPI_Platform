SER421 Lab - GraphQL APIs

I have started with the Original code That I pulled from the official git repo
Then set them up in 2 folders one for Activity 1 and the other for Activity 2.

After having done the tasks for activity 1, I created test_cases in postman, I have additionally tried to test for robust cases like if the id for author is given as a negative integer, I have assumed that if the array returned is empty the specific id being tested for does not exist.

In other cases where it was an error I have thrown exceptions.

For activity2 I have understood that I need to reimplement the grapple stuff from activity1 but Im sure what the difference between the graphql is as it seems identical

So I have kept only one testcasefile named -> testingGraphqlQueries.json which can be used for both activities and it is returning the same results.

Both activities are managed under separate git repos
