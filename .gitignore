# Old lab activities — superseded by backend/. Kept ignored until removed,
# because each still contains its own nested .git repo.
Activity1/
Activity2/

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Lab assignment PDF
*.pdf

# IDE files
.idea/
.vscode/
*.iml
*.iws
*.ipr

# Java / Gradle build artifacts
# (backend/.gitignore re-includes the Gradle wrapper jar, which must be committed)
.gradle/
build/
bin/
out/
*.class
*.jar
*.war

# Node / frontend
node_modules/
frontend/dist/

# Personal local notes (not tracked for now)
HOW_TO_RUN.md
DEPLOY_AWS.md

# Misc
*.log
*.tmp
booktown-key.pem
